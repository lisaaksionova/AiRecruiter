# AiRecruiter

Платформа для рекрутингу з можливістю підбору кандидатів до вакансій.

## 📋 Зміст
- [Огляд](#огляд)
- [Технологічний стек](#технологічний-стек)
- [Передумови](#передумови)
- [Встановлення](#встановлення)
- [Запуск застосунку](#запуск-застосунку)
  - [Варіант 1: З Docker Compose (Рекомендовано)](#варіант-1-використання-docker-compose-рекомендовано)
  - [Варіант 2: Без Docker (Локальна розробка)](#варіант-2-без-docker-локальна-розробка)
- [Доступ до застосунку](#доступ-до-застосунку)
- [Структура проекту](#структура-проекту)
- [Вирішення проблем](#вирішення-проблем)

## 🎯 Огляд

AiRecruiter - це full-stack веб-застосунок, який використовує штучний інтелект для підбору кандидатів до вакансій. Система складається з трьох основних сервісів:
- **Frontend UI** - користувацький інтерфейс на React
- **Backend API** - REST API на ASP.NET Core
- **Match Service** - AI-движок для підбору на Python

## 🛠 Технологічний стек

- **Frontend**: React 19, TypeScript, Vite, Ant Design
- **Backend**:  ASP.NET Core 9.0, C#
- **AI-сервіс**: Python 3.9, FastAPI, scikit-learn
- **База даних**: PostgreSQL 17
- **Інфраструктура**:  Docker, Docker Compose

## 📦 Передумови

### Для запуску з Docker: 

- **Docker Desktop** (версія 20.10 або вище)
  - [Завантажити для Windows](https://docs.docker.com/desktop/install/windows-install/)
  - [Завантажити для Mac](https://docs.docker.com/desktop/install/mac-install/)
  - [Завантажити для Linux](https://docs.docker.com/desktop/install/linux-install/)
- **Docker Compose** (зазвичай входить до Docker Desktop)
- **Git** (для клонування репозиторію)

### Для запуску без Docker:

- **. NET SDK 9.0** або вище
  - [Завантажити . NET SDK](https://dotnet.microsoft.com/download/dotnet/9.0)
- **Node.js 22** або вище та **npm**
  - [Завантажити Node.js](https://nodejs.org/)
- **Python 3.9** або вище та **pip**
  - [Завантажити Python](https://www.python.org/downloads/)
- **PostgreSQL 17** або вище
  - [Завантажити PostgreSQL](https://www.postgresql.org/download/)
- **Git** (для клонування репозиторію)

### Перевірка встановлення

**Для Docker:**
```bash
docker --version
docker-compose --version
```

**Для локальної розробки:**
```bash
dotnet --version
node --version
npm --version
python --version
psql --version
```

## 🚀 Встановлення

### Крок 1: Клонування репозиторію

```bash
git clone https://github.com/lisaaksionova/AiRecruiter.git
cd AiRecruiter
```

### Крок 2: Перевірка структури проекту

Переконайтеся, що ваш проект має наступну структуру:

```
AiRecruiter/
├── AiRecruiter/
│   ├── AiRecruiter.API/
│   ├── AiRecruiter.BLL/
│   ├── AiRecruiter.DAL/
│   └── Dockerfile
├── AiRecruiter.Ui/
│   └── AiRecruiter.Ui/
│       ├── package.json
│       └── Dockerfile
├── MatchService/
│   ├── app.py
│   ├── requirements.txt
│   └── Dockerfile
└── docker-compose.yml
```

## 🏃 Запуск застосунку

### Варіант 1: Використання Docker Compose (Рекомендовано)

Це найпростіший спосіб запустити весь застосунок з усіма сервісами.  

#### Крок 1: Запуск усіх сервісів

```bash
docker-compose up --build
```

Ця команда: 
- Збирає всі Docker-образи
- Створює та запускає всі контейнери
- Налаштовує мережу та томи
- Ініціалізує базу даних PostgreSQL

**Примітка**: Перша збірка може зайняти 5-10 хвилин залежно від швидкості інтернету.

#### Крок 2: Очікування запуску сервісів

Слідкуйте за логами, щоб переконатися, що всі сервіси працюють.   Ви повинні побачити:
- ✅ Перевірка здоров'я бази даних пройдена
- ✅ Backend API запущено на порту 5087
- ✅ Match Service запущено на порту 8000
- ✅ Frontend UI запущено на порту 5173

#### Крок 3: Перевірка роботи всіх сервісів

```bash
docker-compose ps
```

Ви повинні побачити всі чотири сервіси (ui, api, match-service, db) зі статусом "Up".  

#### Запуск у фоновому режимі

Щоб запустити сервіси у фоні: 

```bash
docker-compose up -d --build
```

Переглянути логи:
```bash
docker-compose logs -f
```

Переглянути логи конкретного сервісу:  
```bash
docker-compose logs -f ui
docker-compose logs -f api
docker-compose logs -f match-service
docker-compose logs -f db
```

---

### Варіант 2: Без Docker (Локальна розробка)

Цей варіант підходить для розробки, коли вам потрібен прямий доступ до коду та швидке перезавантаження.

#### Крок 1: Налаштування бази даних PostgreSQL

##### 1.1. Запуск PostgreSQL

**На Windows:**
```bash
# Після встановлення PostgreSQL запустіть службу через Services
# Або використайте pgAdmin
```

**На Mac:**
```bash
brew services start postgresql@17
```

**На Linux:**
```bash
sudo systemctl start postgresql
sudo systemctl enable postgresql
```

##### 1.2. Створення бази даних

```bash
# Увійдіть в PostgreSQL CLI
psql -U postgres

# Створіть базу даних
CREATE DATABASE AiRecruiter;

# Створіть користувача (опціонально)
CREATE USER airecruiter_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE AiRecruiter TO airecruiter_user;

# Вийдіть з CLI
\q
```

#### Крок 2: Налаштування Backend API

##### 2.1. Перейдіть до директорії API

```bash
cd AiRecruiter/AiRecruiter.API
```

##### 2.2. Налаштуйте рядок підключення до бази даних

Створіть або відредагуйте файл `appsettings.Development.json`:

```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=AiRecruiter;Username=postgres;Password=password"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  }
}
```

**Важливо**: Замініть `password` на ваш реальний пароль PostgreSQL.

##### 2.3. Відновлення пакетів NuGet

```bash
dotnet restore
```

##### 2.4. Застосування міграцій бази даних

```bash
dotnet ef database update
```

Якщо команда `dotnet ef` не знайдена, встановіть EF Core tools:

```bash
dotnet tool install --global dotnet-ef
```

##### 2.5. Запуск Backend API

```bash
dotnet run --urls "http://localhost:5087"
```

або просто:

```bash
dotnet run
```

API буде доступний на http://localhost:5087

**Залиште цей термінал відкритим! **

#### Крок 3: Налаштування Match Service (Python)

##### 3.1. Відкрийте новий термінал і перейдіть до директорії MatchService

```bash
cd MatchService
```

##### 3.2. Створіть віртуальне середовище Python (рекомендовано)

**На Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**На Mac/Linux:**
```bash
python3 -m venv venv
source venv/bin/activate
```

##### 3.3. Встановіть залежності Python

```bash
pip install -r requirements.txt
```

##### 3.4. Налаштуйте змінні середовища (опціонально)

Створіть файл `.env` у директорії `MatchService`:

```env
API_URL=http://localhost:5087
PYTHONDONTWRITEBYTECODE=1
PYTHONUNBUFFERED=1
```

##### 3.5. Запуск Match Service

```bash
uvicorn app: app --host 0.0.0.0 --port 8000 --reload
```

Match Service буде доступний на http://localhost:8000

**Залиште цей термінал відкритим!**

#### Крок 4: Налаштування Frontend UI

##### 4.1. Відкрийте новий термінал і перейдіть до директорії UI

```bash
cd AiRecruiter.Ui/AiRecruiter.Ui
```

##### 4.2. Встановіть залежності npm

```bash
npm install
```

**Примітка**: Перша установка може зайняти кілька хвилин. 

##### 4.3. Налаштуйте змінні середовища

Створіть файл `.env` у директорії `AiRecruiter.Ui/AiRecruiter.Ui`:

```env
VITE_API_URL=http://localhost:5087
VITE_MATCHER_URL=http://localhost:8000
```

##### 4.4. Запуск Frontend (Development Server)

```bash
npm run dev
```

Frontend буде доступний на http://localhost:5173

**Залиште цей термінал відкритим!**

#### Крок 5: Перевірка запуску

Тепер у вас має бути відкрито **3 термінали**: 
1. ✅ Backend API на порту 5087
2. ✅ Match Service на порту 8000
3. ✅ Frontend UI на порту 5173

Відкрийте браузер і перейдіть на http://localhost:5173

#### Зупинка сервісів

Для зупинки кожного сервісу натисніть `Ctrl + C` у відповідному терміналі. 

Для деактивації Python віртуального середовища: 
```bash
deactivate
```

---

## 🌐 Доступ до застосунку

Після запуску всіх сервісів ви можете отримати доступ до:  

- **Frontend-застосунок**: http://localhost:5173
- **Backend API**: http://localhost:5087
- **AI Match Service**: http://localhost:8000
- **База даних PostgreSQL**: localhost:5432
  - База даних: `AiRecruiter`
  - Ім'я користувача: `postgres`
  - Пароль: `password` (або ваш власний)

### API Endpoints

Ви можете тестувати API endpoints використовуючи: 
- **Swagger UI**: http://localhost:5087/swagger (якщо налаштовано)
- **Postman** або **curl**

Приклад: 
```bash
curl http://localhost:5087/api/health
```

## 📁 Структура проекту

### Backend API (AiRecruiter)
- **AiRecruiter.API** - Контролери Web API та endpoints
- **AiRecruiter.BLL** - Рівень бізнес-логіки
- **AiRecruiter. DAL** - Рівень доступу до даних з Entity Framework Core

### Frontend (AiRecruiter.Ui)
- Побудовано на React 19 та TypeScript
- Використовує Vite для швидкої розробки
- Бібліотека компонентів Ant Design
- React Router для навігації
- Zustand для управління станом

### AI-сервіс (MatchService)
- Мікросервіс на FastAPI
- scikit-learn для ML алгоритмів підбору
- NumPy для числових операцій

## 🛑 Зупинка застосунку

### При використанні Docker: 

#### Зупинити всі сервіси

```bash
docker-compose down
```

#### Зупинити та видалити томи (дані бази даних)

```bash
docker-compose down -v
```

**Попередження**: Це видалить всі дані в базі даних!

#### Зупинити конкретний сервіс

```bash
docker-compose stop ui
docker-compose stop api
docker-compose stop match-service
docker-compose stop db
```

### При локальному запуску: 

Натисніть `Ctrl + C` в кожному терміналі для зупинки сервісів. 

Для зупинки PostgreSQL: 

**На Mac:**
```bash
brew services stop postgresql@17
```

**На Linux:**
```bash
sudo systemctl stop postgresql
```

## 🔧 Вирішення проблем

### Загальні проблеми

#### Проблема:  Порт вже використовується

Якщо ви бачите помилку "port is already allocated" або "Address already in use": 

```bash
# Знайти процес, що використовує порт (приклад для порту 5173)
# На Windows: 
netstat -ano | findstr :5173

# На Mac/Linux:  
lsof -i : 5173

# Завершити процес або змінити порт
```

### Проблеми з Docker

#### Проблема: Помилки підключення до бази даних

Якщо API не може підключитися до бази даних:

1. Перевірте, чи контейнер бази даних здоровий: 
   ```bash
   docker-compose ps
   ```

2. Перезапустіть базу даних:
   ```bash
   docker-compose restart db
   ```

3. Перевірте логи бази даних:
   ```bash
   docker-compose logs db
   ```

#### Проблема: Помилки збірки

Якщо збірка Docker не вдається:

1. Очистіть кеш Docker: 
   ```bash
   docker-compose build --no-cache
   ```

2. Видаліть старі контейнери та томи:
   ```bash
   docker-compose down -v
   docker system prune -a
   ```

3. Перезберіть:  
   ```bash
   docker-compose up --build
   ```

#### Проблема: Frontend не може підключитися до Backend

1. Перевірте, що всі сервіси працюють:
   ```bash
   docker-compose ps
   ```

2. Перевірте змінні середовища в `docker-compose.yml`:
   - `VITE_API_URL=http://api:5087`
   - `VITE_MATCHER_URL=http://match-service:8000`

3. Перезапустіть UI сервіс:
   ```bash
   docker-compose restart ui
   ```

#### Проблема: Помилки Node Modules або залежностей

1. Видаліть том node_modules:
   ```bash
   docker-compose down -v
   docker volume rm airecruiter_ui_node_modules
   ```

2. Перезберіть:
   ```bash
   docker-compose up --build
   ```

### Проблеми при локальному запуску

#### Проблема: dotnet ef не знайдено

```bash
dotnet tool install --global dotnet-ef
# Додайте шлях до PATH якщо потрібно
```

#### Проблема:  Помилка підключення до PostgreSQL

1. Перевірте, чи запущена служба PostgreSQL:
   ```bash
   # Windows (в PowerShell як адміністратор):
   Get-Service postgresql*
   
   # Mac: 
   brew services list
   
   # Linux:
   sudo systemctl status postgresql
   ```

2. Перевірте рядок підключення в `appsettings.Development.json`

3. Перевірте пароль PostgreSQL: 
   ```bash
   psql -U postgres -d AiRecruiter
   ```

#### Проблема: Python модулі не встановлюються

1. Оновіть pip:
   ```bash
   python -m pip install --upgrade pip
   ```

2. Переконайтеся, що віртуальне середовище активоване

3. Встановіть залежності знову:
   ```bash
   pip install -r requirements.txt --no-cache-dir
   ```

#### Проблема: npm install помилки

1. Очистіть кеш npm: 
   ```bash
   npm cache clean --force
   ```

2. Видаліть node_modules та package-lock.json:
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```

#### Проблема:  CORS помилки у Frontend

Переконайтеся, що у вашому Backend API налаштовано CORS.  У файлі `Program.cs` або `Startup.cs`:

```csharp
builder.Services. AddCors(options =>
{
    options.AddPolicy("AllowFrontend",
        builder => builder
            .WithOrigins("http://localhost:5173")
            .AllowAnyMethod()
            .AllowAnyHeader());
});

// Після app.Build():
app.UseCors("AllowFrontend");
```

#### Проблема: Міграції Entity Framework не застосовуються

```bash
# Перевірте список міграцій
dotnet ef migrations list

# Створіть нову міграцію (якщо потрібно)
dotnet ef migrations add InitialCreate

# Застосуйте міграції
dotnet ef database update

# Якщо потрібно скинути базу даних
dotnet ef database drop
dotnet ef database update
```

## 🔄 Робочий процес розробки

### З Docker: 

Для розробки сервіси використовують volume mounts, тому зміни відображаються автоматично:  

- **Frontend**:  Зміни автоматично перезавантажуються (hot-reload)
- **Backend**: Перезапустіть API сервіс
  ```bash
  docker-compose restart api
  ```
- **Match Service**: Перезапустіть match service
  ```bash
  docker-compose restart match-service
  ```

### При локальному запуску:

- **Frontend**: Vite автоматично перезавантажує зміни (hot-reload)
- **Backend**: Натисніть `Ctrl + C` та перезапустіть `dotnet run`, або використовуйте `dotnet watch run` для автоматичного перезавантаження
- **Match Service**: Uvicorn з прапором `--reload` автоматично перезавантажує зміни

### Автоматичне перезавантаження Backend: 

```bash
cd AiRecruiter/AiRecruiter.API
dotnet watch run
```

### Перегляд логів

**З Docker:**
```bash
# Всі сервіси
docker-compose logs -f

# Конкретний сервіс
docker-compose logs -f api
```

**Локально:**
Логи відображаються безпосередньо в терміналі кожного сервісу. 

## 📊 Управління базою даних

### Доступ до PostgreSQL CLI

**З Docker:**
```bash
docker-compose exec db psql -U postgres -d AiRecruiter
```

**Локально:**
```bash
psql -U postgres -d AiRecruiter
```

### Корисні команди PostgreSQL: 

```sql
-- Переглянути список таблиць
\dt

-- Переглянути структуру таблиці
\d table_name

-- Переглянути всі бази даних
\l

-- Вийти
\q
```

### Резервне копіювання бази даних

**З Docker:**
```bash
docker-compose exec db pg_dump -U postgres AiRecruiter > backup.sql
```

**Локально:**
```bash
pg_dump -U postgres AiRecruiter > backup.sql
```

### Відновлення бази даних

**З Docker:**
```bash
docker-compose exec -T db psql -U postgres AiRecruiter < backup.sql
```

**Локально:**
```bash
psql -U postgres AiRecruiter < backup.sql
```

## 🧪 Тестування

### Тестування Backend API

**З Docker:**
```bash
# Увійти в контейнер API
docker-compose exec api bash

# Запустити тести (якщо налаштовано)
dotnet test
```

**Локально:**
```bash
cd AiRecruiter
dotnet test
```

### Тестування Frontend

**З Docker:**
```bash
# Увійти в контейнер UI
docker-compose exec ui sh

# Запустити тести
npm test
```

**Локально:**
```bash
cd AiRecruiter.Ui/AiRecruiter.Ui
npm test
```

### Тестування Match Service

**Локально:**
```bash
cd MatchService
# Встановіть pytest якщо є тести
pip install pytest
pytest
```

## 📝 Змінні середовища

### З Docker (docker-compose.yml):

#### Frontend (UI)
- `VITE_API_URL` - URL Backend API
- `VITE_MATCHER_URL` - URL Match Service
- `NODE_ENV` - Середовище Node (development/production)

#### Backend (API)
- `ASPNETCORE_ENVIRONMENT` - Середовище ASP.NET
- `ASPNETCORE_URLS` - URL для прослуховування
- `ConnectionStrings__DefaultConnection` - Рядок підключення PostgreSQL

#### Match Service
- `PYTHONDONTWRITEBYTECODE` - Запобігання створенню . pyc файлів
- `PYTHONUNBUFFERED` - Небуферизований вивід Python
- `API_URL` - URL Backend API

#### База даних
- `POSTGRES_USER` - Користувач бази даних
- `POSTGRES_PASSWORD` - Пароль бази даних
- `POSTGRES_DB` - Ім'я бази даних

### При локальному запуску: 

#### Frontend (. env файл)
```env
VITE_API_URL=http://localhost:5087
VITE_MATCHER_URL=http://localhost:8000
```

#### Backend (appsettings.Development.json)
```json
{
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=AiRecruiter;Username=postgres;Password=your_password"
  }
}
```

#### Match Service (. env файл, опціонально)
```env
API_URL=http://localhost:5087
PYTHONDONTWRITEBYTECODE=1
PYTHONUNBUFFERED=1
```

## 🤝 Внесок у проект

1.  Зробіть Fork репозиторію
2. Створіть гілку для нової функції:   `git checkout -b feature/my-feature`
3. Зробіть коміт змін:  `git commit -am 'Додати нову функцію'`
4. Відправте зміни в гілку: `git push origin feature/my-feature`
5. Створіть Pull Request

## 📄 Ліцензія

Цей проект ліцензовано під ліцензією MIT - дивіться файл LICENSE для деталей.

## 📧 Підтримка

Для питань та проблем: 
- Створіть issue на GitHub
- Контакт: [your-email@example.com]

---

**Успішного рекрутингу!  🎉**
